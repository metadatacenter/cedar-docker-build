FROM metadatacenter/cedar-microservice:2.9.2-SNAPSHOT

ENV CEDAR_SERVER_NAME=artifact

ENV CEDAR_ARTIFACT_HTTP_PORT=9001
ENV CEDAR_ARTIFACT_ADMIN_PORT=9101
ENV CEDAR_ARTIFACT_STOP_PORT=9201

ENV LOGDIR=${CEDAR_HOME}/log/server-${CEDAR_SERVER_NAME}/
RUN mkdir -p ${LOGDIR}
VOLUME ${LOGDIR}

EXPOSE ${CEDAR_ARTIFACT_HTTP_PORT} ${CEDAR_ARTIFACT_ADMIN_PORT} ${CEDAR_ARTIFACT_STOP_PORT}

# Empty unless bin/stage-local-jar.sh has staged a locally built jar here.
COPY local/ ${CEDAR_HOME}/local/

RUN ${CEDAR_HOME}/install_deps.sh
